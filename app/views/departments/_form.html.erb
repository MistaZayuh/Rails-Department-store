<h1><%= @department.id ? "Edit Department" : "New Department" %>
<hr />
<%= form_for @department do |f| %>
<%= f.label :name %>
<%= f.text_field :name, required: true, autofocus: true %>

<%= f.submit %>
<% end %>